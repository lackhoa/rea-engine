#load "boolean.rea"
#load "nat.rea"


check truth: False = (2*3 = 5);  ;; todo: this doesn't work due to typing of "=".

;; theorem plusCommutative(n, m: Nat)
;; : n + m = m + n
;; {
;;  switch n;
;;  - switch m; - return truth; -
;;  }
