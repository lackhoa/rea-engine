#load "boolean.rea"

theorem easyClap(a: Bool) : Bool
{
 return a;
 }
print(easyClap);
print(easyClap(T));
print(easyClap(F));

theorem stillEasy(a: Bool) : (F & a = F)
{
 return truth;
 }

theorem notSoEasy(a: Bool) : (a & F = F)
{
 switch a;
 - return truth;
 - return truth;
 }

print notSoEasy;

theorem andCommutative(a: Bool, b: Bool, c: Bool) : (a & (b & c) = (a & b) & c)
{
 switch a;
 - return truth;
 - return truth;
 }
